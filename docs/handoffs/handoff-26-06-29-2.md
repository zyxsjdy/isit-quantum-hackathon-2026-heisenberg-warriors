# Handoff 26-06-29-2

## Context

Repo: `C:\Users\harry\gitrepo\isit-quantum-hackathon-2026-heisenberg-warriors`

The user wants the project pushed toward a competition-winning QAOA-ISAC UAV deployment result. The prior handoff said the original `qaoa_isac_notebook.ipynb` was a working prototype but too small because greedy tied the exact/QAOA result. This session improved the benchmark evidence while preserving the original notebook.

## Current Artifacts

- Original notebook, preserved: `qaoa_isac_notebook.ipynb`
- Benchmark harness: `qaoa_isac_benchmark.py`
- Runnable benchmark notebook: `qaoa_isac_benchmark.ipynb`
- Benchmark results: `qaoa_isac_benchmark_results.json`

Do not delete `qaoa_isac_notebook.ipynb`; the user explicitly asked to keep it.

## What Changed

- Added/updated the benchmark harness with:
  - valid-subspace QAOA over feasible UAV-grid assignments,
  - QAOA top-K plus local-search polishing,
  - random top-K plus local-search baseline,
  - multi-seed suite support.
- Converted the benchmark flow into `qaoa_isac_benchmark.ipynb` so it can be run and shown in notebook form.
- Added a guarded IBM Quantum hardware section in the benchmark notebook:
  - `SUBMIT_TO_HARDWARE = False` by default,
  - builds a full-binary QUBO QAOA circuit,
  - includes IBM Runtime `SamplerV2` submission code and post-processing helpers.
- Regenerated `qaoa_isac_benchmark_results.json`.

## Current Result

Headline benchmark in `qaoa_isac_benchmark_results.json`:

- Scenario: `U=4`, `G=6`, `S=6`, `Nt=4`, full-binary size `24` qubits, seed `67`.
- Exact/QAOA top assignment reaches `AR_rate=1.0`.
- Greedy is feasible but lower at `AR_rate=0.811`.
- QAOA optimum probability is about `6.25x` uniform feasible sampling.
- QAOA top-8 plus local search reaches the exact optimum; random top-8 plus local-search mean AR is about `0.988`.

Multi-seed suite in the same JSON:

- Suite scenario: `U=3`, `G=6`, `S=5`, seeds `1:60`, top-K `8`.
- Evaluated seeds: `25`.
- Mean greedy AR: about `0.896`.
- Mean QAOA top-8 plus local-search AR: about `0.999`.
- Stress seeds where greedy gap is at least 10%: `12`; QAOA top-8 plus local search hit optimum on all `12`.

## Verification Done

Successful:

```powershell
& 'C:\Users\harry\.conda\envs\qiskit\python.exe' -X utf8 .\qaoa_isac_benchmark.py --include-suite --grid-steps 81 --random-trials 16
& 'C:\Users\harry\.conda\envs\qiskit\python.exe' -X utf8 -m py_compile .\qaoa_isac_benchmark.py
```

Also executed all code cells from `qaoa_isac_benchmark.ipynb` through a Python harness with hardware submission disabled. The hardware candidate circuit built successfully:

- `24` qubits
- depth `139` before ISA transpilation
- `552` CX gates before ISA transpilation

`nbconvert --execute --inplace qaoa_isac_benchmark.ipynb` failed because the registered `python3` kernelspec points to base Anaconda, which does not have Qiskit. Available kernels checked:

- `python3`: base Anaconda, missing Qiskit
- `qd-stage3`: points to `C:\Users\harry\.conda\envs\qd_26\python.exe`

The working interpreter remains:

```powershell
C:\Users\harry\.conda\envs\qiskit\python.exe
```

## Known Caveats

- The strongest benchmark is still a valid-subspace QAOA simulation / QAOA-guided sampler, not real hardware data.
- The hardware section builds a full-binary QUBO QAOA circuit; it does not implement the custom valid-subspace mixer on hardware.
- `qiskit-ibm-runtime` was not installed in the checked qiskit environment during this session.
- No IBM Quantum API key or credential was added to the repo.
- The multi-seed suite skips seeds with too few feasible states or infeasible greedy comparisons; this is reported in the JSON and notebook.
- Recurring Git warning remains: `unable to access 'C:\Users\harry/.config/git/ignore': Permission denied`.

## Next Best Work

1. Register a Jupyter kernelspec for `C:\Users\harry\.conda\envs\qiskit\python.exe`, then execute `qaoa_isac_benchmark.ipynb` in-place so saved notebook outputs are visible for judges.
2. Decide whether to install `qiskit-ibm-runtime` and submit a small real-hardware job. If doing so, keep `SUBMIT_TO_HARDWARE = False` until credentials, backend, and cost/queue implications are confirmed.
3. Add a short competition-facing explanation of the claim:
   - exact feasible reference,
   - feasible greedy baseline,
   - QAOA top state,
   - QAOA top-K plus local search,
   - random top-K plus local search,
   - caveat that hardware path is full-binary QUBO rather than the custom valid mixer.
4. If time allows, add a stronger classical baseline such as simulated annealing or multi-start local search to reduce reviewer skepticism.
5. Commit the benchmark artifacts once the user approves.

## Suggested Skills

- `diagnosing-bugs`: use if Jupyter kernel registration, notebook execution, or Qiskit Runtime setup fails.
- `grill-with-docs`: use to stress-test the competition claim and write an ADR/domain note if the framing changes.
- `tdd`: use if extracting benchmark logic into tested modules.
- `github:yeet`: use if the next step is committing/pushing/opening a PR.
- `conventional-commits`: use if writing a multi-line commit message for the benchmark and notebook changes.
