# Handoff 26-06-30-1

## Context

Repo: `C:\Users\harry\gitrepo\isit-quantum-hackathon-2026-heisenberg-warriors`

The user wants to pause now and resume later with a strict effort to fix the
remaining limitations, not just polish the current story. They are currently
waiting for IBM hardware results from `qaoa_isac_benchmark.ipynb`.

Important instruction from the user: do not touch `qaoa_isac_benchmark.ipynb`
for now while hardware results are pending.

The user also added `QAOA_ISAC_UAV.ipynb` as a colleague reference notebook.
Treat it as reference material only unless the user asks to integrate it.

## Current Artifacts To Reference

- Main benchmark harness: `qaoa_isac_benchmark.py`
- Judge-facing benchmark notebook: `qaoa_isac_benchmark.ipynb`
- Latest benchmark JSON: `qaoa_isac_benchmark_results.json`
- Generated figures: `figures/`
- Submission-facing summary: `README.md`
- IBM hardware guide: `docs/ibm-hardware-run.md`
- Original preserved notebook: `qaoa_isac_notebook.ipynb`
- Colleague reference notebook: `QAOA_ISAC_UAV.ipynb`
- Previous handoff: `docs/handoffs/handoff-26-06-29-2.md`

Do not duplicate the detailed metrics or hardware instructions already present
in those files. Read them directly.

## Current Verified Result Snapshot

From `qaoa_isac_benchmark_results.json`:

- Headline QAOA AR: `1.0`
- Headline greedy AR: `0.8111447260708325`
- Suite QAOA top-K + local mean AR: `0.9992119144089444`
- Suite simulated annealing mean AR: `0.9864857255953211`
- Stress QAOA top-K + local mean AR: `1.0`
- Stress simulated annealing mean AR: `0.9900098544349096`
- Figure count: `5`

The result is good, but the user correctly feels the limitations are serious.
The next session should focus on reducing those limitations rather than adding
surface polish.

## Strict Limitations / Risks

1. The headline result is not a real hardware result. It is a valid-subspace
   QAOA simulation / QAOA-guided candidate sampler.

2. The hardware section is a different algorithmic path. It uses a full-binary
   QUBO circuit with one qubit per UAV-grid variable, not the custom
   valid-subspace mixer from the headline simulator.

3. Scale is small. The headline problem is `U=4, G=6` with 24 full-binary
   variables and 329 feasible assignments. The hardware demo path is expected
   to be smaller, around `U=3, G=6` / 18 qubits.

4. The best reported workflow includes local search. Judges may ask how much of
   the win comes from quantum probability concentration versus classical
   polishing.

5. Simulated annealing is close. QAOA still wins in the current metrics, but SA
   reaches high AR, especially on stress cases.

6. Hardware evidence is pending. Until the IBM job returns, there is no measured
   hardware feasible-rate, best feasible assignment, AR, backend name, job ID,
   depth, or CX count in the final story.

7. There is no noise robustness story yet. Hardware noise may reduce feasibility
   or objective quality, and post-processing may dominate the final feasible
   result.

8. `QAOA_ISAC_UAV.ipynb` has serious correctness red flags if used as evidence:
   tiny `U=2, G=4` scale, saved `QP verification: FAIL`, a brute-force
   reference with `C3=False`, and greedy rate exceeding the claimed brute-force
   optimum. Use it only as a structure/plot reference unless fixed.

## Recommended Next Work

1. Wait for the hardware job/status from `qaoa_isac_benchmark.ipynb`. Do not edit
   that notebook until the user confirms the job is complete or abandoned.

2. Once hardware returns, extract a hardware result table:
   backend name, job ID, qubit count, depth, CX count, shots, feasible sample
   rate, best feasible bitstring/assignment, best hardware AR, greedy AR on the
   same hardware scenario, and whether hardware beats random/greedy.

3. Decide whether to keep the hardware result. If it is weak, present it only as
   feasibility evidence and keep the headline on the validated simulator.

4. If time allows, reduce the hardware/simulator mismatch:
   implement a small valid-subspace mixer circuit for `U=2` or `U=3`, or clearly
   document why the hardware run is a full-binary QUBO bridge rather than the
   exact custom mixer.

5. Strengthen the quantum contribution argument:
   compare QAOA top-K candidate ranking against simulated annealing and random
   with equal candidate budgets and equal local-search polishing budget.

6. Fix or quarantine `QAOA_ISAC_UAV.ipynb` before presentation. If reused,
   correct the QP verification issue, enforce `C3` consistently, and ensure the
   optimum reference is based on exact feasible rate rather than QUBO-only
   objective.

7. After results are confirmed, update presentation materials and add team
   contribution names. The user explicitly wants to do team contributions later.

## Current Working Tree Notes

Observed before this handoff:

- `qaoa_isac_benchmark.ipynb` is modified. Do not touch it while hardware is
  pending.
- `QAOA_ISAC_UAV.ipynb` is untracked and came from the user/colleague.
- Git sometimes warns: `unable to access 'C:\Users\harry/.config/git/ignore':
  Permission denied`.

Do not run destructive git commands or stage files automatically.

## Suggested Skills

- `diagnosing-bugs`: use if hardware job retrieval, runtime imports, notebook
  execution, or `QAOA_ISAC_UAV.ipynb` correctness checks fail.
- `tdd`: use if extracting hardware post-processing or baseline comparisons
  into testable functions.
- `karpathy-code-guideline`: use for surgical fixes to the benchmark without
  overengineering.
- `grill-with-docs`: use after hardware results return to stress-test the final
  claim and decide the honest presentation framing.
- `handoff`: use again before switching agents or after hardware results are
  captured.
