# Handoff 26-06-30-2

## Context

Repo: `C:\Users\harry\gitrepo\isit-quantum-hackathon-2026-heisenberg-warriors`

The user is pausing and will continue later. The current focus is improving the
competition story after a completed IBM hardware run, not adding surface polish.

The notebook now directly displays the completed hardware result. Do not submit
a new IBM job unless the user explicitly decides to run the next hardware
experiment.

## Current Important Artifacts

- Main notebook: `qaoa_isac_benchmark.ipynb`
- Benchmark harness: `qaoa_isac_benchmark.py`
- Current result JSON: `qaoa_isac_benchmark_results.json`
- Hardware guide: `docs/ibm-hardware-run.md`
- Submission summary: `README.md`
- Focused tests: `tests/test_benchmark_diagnostics.py`
- Previous handoff: `docs/handoffs/handoff-26-06-30-1.md`

## What Changed In This Session

- Fixed IBM Runtime count extraction for `SamplerV2` results whose classical
  register is named `c` instead of `meas`.
- Updated `qaoa_isac_benchmark.ipynb` so the final hardware cell retrieves the
  completed IBM job and prints the visible hardware summary.
- Kept `SUBMIT_TO_HARDWARE = False` in the notebook to avoid accidental
  resubmission.
- Added hardware helper functions in `qaoa_isac_benchmark.py`, including
  register-agnostic count extraction and full-binary QAOA circuit summary.
- Added and recorded an 18-qubit next hardware candidate:
  `U=3, G=6, S=5`, seed `35`.
- Updated `README.md`, `docs/ibm-hardware-run.md`, and
  `qaoa_isac_benchmark_results.json` with measured hardware evidence and the
  next smaller hardware target.
- Added focused regression tests in `tests/test_benchmark_diagnostics.py`.

## Verified Current Hardware Result

Completed IBM job:

- Backend: `ibm_quebec`
- Job ID: `d91i01vccmks73d56i80`
- Register: `c`
- Shots: `1024`
- Raw feasible samples: `1/1024`
- Best raw feasible assignment: `[1, 5, 0, 3]`, AR `0.829`
- Best projected assignment: `[2, 4, 1, 3]`, AR `1.000`, projected count `16`

Interpretation: this is real hardware execution evidence for the full-binary
QUBO bridge. It is not hardware quantum-advantage evidence, because raw
feasibility is very low and feasible projection/post-processing recovers the
optimum.

In the notebook, the visible result is in the last code cell under:

```markdown
## 7. Optional IBM Quantum Hardware Run
```

Search for `Raw feasible samples` or `Best projected assignment` if the user
cannot find it in Jupyter.

## Next Hardware Candidate

Recorded under `hardware_demo_candidate` in `qaoa_isac_benchmark_results.json`:

- Scenario: `U=3, G=6, S=5`
- Seed: `35`
- Full-binary qubits: `18`
- Feasible assignments: `22`
- Greedy AR: `0.785`
- Valid-subspace QAOA top-state AR: `1.000`
- Valid-subspace QAOA optimum probability: `0.358`
- Pre-ISA circuit depth: `103`
- Pre-ISA CX gates: `306`

This improves over the completed 24-qubit hardware bridge before ISA
transpilation:

- Old: 24 qubits, depth `139`, `552` CX gates
- New candidate: 18 qubits, depth `103`, `306` CX gates

## Recommended Next Work

1. Add a random-bitstring projection baseline for the completed 24-qubit IBM
   result. This answers whether hardware sampling beat projection from random
   bitstrings.
2. Optimize angles for the smaller 18-qubit full-binary QUBO circuit. Do not
   reuse only the valid-subspace angles if time allows.
3. Transpile the 18-qubit candidate for the selected IBM backend and inspect
   ISA depth/two-qubit gates before submitting.
4. If the transpiled circuit is meaningfully smaller than the 24-qubit run,
   submit one new IBM job with `SUBMIT_TO_HARDWARE = True`.
5. Update the final presentation:
   - headline win: valid-subspace QAOA candidate efficiency
   - support: multi-seed/stress suite and SA/random comparisons
   - hardware: real-device feasibility, with honest raw-feasibility caveat

## Current Verification

Ran successfully:

- `python -m py_compile .\qaoa_isac_benchmark.py`
- `python -m unittest tests.test_benchmark_diagnostics`
- `python -m json.tool .\qaoa_isac_benchmark.ipynb`
- `python -m json.tool .\qaoa_isac_benchmark_results.json`

Git warned about `C:\Users\harry/.config/git/ignore` permission, but this did
not block the work.

## Working Tree Notes

Modified files observed:

- `README.md`
- `docs/ibm-hardware-run.md`
- `qaoa_isac_benchmark.ipynb`
- `qaoa_isac_benchmark.py`
- `qaoa_isac_benchmark_results.json`
- `tests/test_benchmark_diagnostics.py`

Do not run destructive git commands or stage files automatically.

## Suggested Skills

- `diagnosing-bugs`: use if notebook output is not visible, IBM job retrieval
  fails, or Runtime result shape changes again.
- `tdd`: use for random-projection baseline and angle-optimization helpers.
- `karpathy-code-guideline`: use to keep hardware improvements surgical.
- `grill-with-docs`: use before final submission to stress-test the claims and
  wording against judge questions.
- `handoff`: use again before switching agents or after a new hardware job.
